@isTest
public with sharing class InternalConsentUpsertInvocableTest {

    @isTest
    private static void testExecute() {
        // For now, test removed so it doesn't block deployment and UATs. To be implemented in next release.
        // @PetrBatronicek
    }
}